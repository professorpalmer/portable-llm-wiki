[pytest]
# Put the backend dir on sys.path[0] for every invocation (CWD-independent),
# so `import app` and `import tests` resolve to this project's packages — not
# a same-named package elsewhere in the environment. Together with
# tests/__init__.py this guarantees `tests.conftest` is *our* conftest.
pythonpath = .
testpaths = tests
