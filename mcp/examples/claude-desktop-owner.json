{
  "_comment": "Merge into ~/Library/Application Support/Claude/claude_desktop_config.json (macOS). Fully quit + relaunch Claude Desktop after edits.",
  "mcpServers": {
    "portable-llm-wiki": {
      "command": "node",
      "args": ["/absolute/path/to/portable-llm-wiki/mcp/dist/server.js"],
      "env": {
        "WIKI_API_BASE": "http://localhost:8000",
        "WIKI_OWNER_TOKEN": "<paste OWNER_TOKEN from backend/.env>"
      }
    }
  }
}
